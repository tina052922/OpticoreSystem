/** localStorage key for the campus-wide selected academic period (all shells share this). */
export const SEMESTER_FILTER_STORAGE_KEY = "opticore.selectedAcademicPeriodId";

/** URL query param for deep links / shareable semester selection. */
export const SEMESTER_FILTER_URL_PARAM = "periodId";
