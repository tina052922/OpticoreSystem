# dependencies
node_modules/

# next.js
.next/
out/

# env (never commit secrets)
.env
.env.local
.env.*.local

# debug / os
npm-debug.log*
.DS_Store

# TypeScript incremental (local; avoid noisy diffs after every build)
tsconfig.tsbuildinfo
