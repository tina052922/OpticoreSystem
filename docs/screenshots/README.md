# OptiCore UI Screenshots (Insert Captures Here)

This folder is referenced by the Use Case Diagrams section in `docs/METHODOLOGY_PAPER_SECTIONS.md`.

Replace the placeholder image filenames with **actual screenshots from the running OptiCore system** (use sanitized data).

## Suggested capture routes (quick list)

- Login: `/login`
- Chairman: `/chairman/dashboard`, `/chairman/ins/faculty`, `/chairman/evaluator`
- College Admin: `/admin/college`, `/admin/college/evaluator`, `/admin/college/schedule-change-requests`, `/admin/college/access-requests`
- GEC: `/admin/gec`, `/admin/gec/request-access`, `/admin/gec/vacant-slots`
- DOI: `/doi/dashboard`, `/doi/evaluator`, `/doi/reviews`, `/doi/schedule-hub`, `/doi/audit-log`
- Instructor: `/faculty`, `/faculty/schedule`, `/faculty/announcements`
- Student: `/student`, `/student/schedule`, `/student/announcements`

